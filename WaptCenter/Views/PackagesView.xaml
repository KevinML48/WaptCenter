<UserControl x:Class="WaptCenter.Views.PackagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Paquets cd48"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="#1D2940" />
            <TextBlock Text="Chargement direct via l'API WAPT en .NET, puis filtrage initial sur le nom des paquets cd48."
                       Margin="0,8,0,0"
                       TextWrapping="Wrap"
                       Foreground="#5E6B84"
                       FontSize="13" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,24,0,18">
            <Button Width="180"
                    Height="40"
                    Padding="18,10"
                    HorizontalAlignment="Left"
                    Background="#1F5A9A"
                    BorderThickness="0"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Command="{Binding LoadPackagesCommand}"
                    Content="Charger les paquets" />

            <TextBlock Margin="0,14,0,0"
                       TextWrapping="Wrap"
                       Foreground="#38506D"
                       Text="{Binding StatusMessage}" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  ItemsSource="{Binding Packages}"
                  RowHeaderWidth="0"
                  GridLinesVisibility="Horizontal"
                  BorderBrush="#D9E0EA"
                  BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Name}" Width="220" />
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="160" />
                <DataGridTextColumn Header="Architecture" Binding="{Binding Architecture}" Width="140" />
                <DataGridTextColumn Header="Maturite" Binding="{Binding Maturity}" Width="120" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
